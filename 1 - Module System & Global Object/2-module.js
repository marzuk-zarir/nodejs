/**
 * ! module:
 * * module means a dot js file which is encapsulate from other files
 * * in browser no files are encapsulated...that's why we should use 'IFFY' in vanilla js
 * * but not nodejs is encapsulate our files automatically
 */

/**
 * ? There are 3 kinds of modules:
 * * 1. core/builtin module - those modules are come from nodejs like fs, os, http, path etc
 * * 2. local module - those modules are project specific and created by users
 * * 3. external/3rd party module - those modules are made before for write less code like lodash, axios, express etc
 */
